// function fun1() {
//   var ido = document.getElementById('mySelect').selectedIndex;
//   var options = document.getElementById('mySelect').options;
//   alert('Selected option ' + options[ido].text);
// }

function fun2(){
  var rng = document.getElementById('r1');
  var div = document.getElementById('test');
  div.style.width = rng.value+'px';
  // i1.value = rng.value;  //для параграфа p.innerHTML
}
